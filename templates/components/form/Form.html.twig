{{ form_start(form, {attr: {novalidate: 'novalidate'}}) }}
{% block content %}

{% endblock %}
<div class="row justify-content-center">
    <div class="col-2 text-center">
        <button type="submit" role="button" class="btn btn-primary">{{ 'form.action.submit' | trans }}</button>
    </div>
</div>
{{ form_end(form) }}