<div class="card m-3 property" >
    <div class="card-body text-center">
        <h5 class="card-title">{{ name }}</h5>
        <p class="card-text">{{ address | raw }}</p>
        <a href="{{ path('user_property_show', {id: id}) }}" class="card-link"><i class="far fa-eye"></i></a>
    </div>
</div>