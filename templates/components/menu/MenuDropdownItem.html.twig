<li class="nav-item">
    <button class="dropdown-item d-flex align-item-centers">
        <a class="nav-link" href="{{ path(route) }}">{{ icon is same as '' ? icon : (('<span class="mx-2">' ~ icon ~ '</span>') | raw) }}{{ label | trans }}</a>
    </button>
</li>
